# Core Dependencies
nflreadpy>=0.1.0
polars>=0.20.0
pandas>=2.0.0
pyarrow>=24.0.0
numpy>=1.24.0  # Python 3.10-3.12 uses numpy 1.x, Python 3.13+ uses numpy 2.x

# Machine Learning (for future phases)
xgboost>=2.0.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
optuna>=3.0.0

# API & Web
fastapi>=0.100.0
uvicorn>=0.23.0
requests>=2.31.0
requests-cache>=1.1.0

# Testing & Code Quality
# Dev/CI tools (pytest, black, ruff, isort, mypy, bandit, ...) are pinned in
# requirements-dev.txt so local, pre-commit and CI runs stay reproducible.
# Install with: pip install -r requirements-dev.txt

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
loguru>=0.7.0
tqdm>=4.66.0
psutil>=5.9.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Data & Config
PyYAML>=6.0.0
joblib>=1.3.0

